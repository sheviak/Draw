<UserControl x:Class="Draw.UserControls.CanvasTemplate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Draw.UserControls"
             mc:Ignorable="d" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             d:DesignHeight="350" d:DesignWidth="280">
    
    <UserControl.Resources>
        <Style x:Key="canvas" TargetType="Canvas">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="Background" Value="White" />
            <Setter Property="Height" Value="280" />
            <Setter Property="Width" Value="280" />
        </Style>
    </UserControl.Resources>
    
    <GroupBox Style="{DynamicResource MaterialDesignCardGroupBox}" Margin="16">
        <GroupBox.HeaderTemplate>
            <DataTemplate>
                    <materialDesign:PackIcon Kind="Number1" Height="24" Width="24" VerticalAlignment="Center" HorizontalAlignment="Center" />
            </DataTemplate>
        </GroupBox.HeaderTemplate>
       
    </GroupBox>
</UserControl>
